const Error = () => {
	return (
		<p className="error font-primary font-bold mt-52 text-lg text-red-500">
            Sorry, we could not load the page you requested. Please try again later
		</p>
	);
};

export default Error;